import { Box } from "@mui/material"
import { useSession } from "next-auth/react";

const Bookings=()=>{
   
    return(
        <Box>
            bookings
        </Box>
    )
}

export default Bookings;